flax>=0.7.2
jax>=0.5,<0.6
jraph>=0.0.6.dev0
jaxtyping>=0.2.21
numpy>=1.25.2,<2
einops>=0.6.1
matplotlib>=3.7.2
tqdm>=4.66.1
tensorflow-probability>=0.21.0
optax>=0.1.7
scipy>=1.11.2
wandb>=0.15.8,<0.21.0
pyyaml>=6.0.1
orbax_checkpoint>=0.3.5,<=0.11.16
seaborn>=0.12.2
pandas>=2.0
rich>=13.5.3
colour>=0.1.5
control>=0.9.4
h5py>=3.11.0
distrax>=0.1.3
gym>=0.26.0
# runtime dependencies of jaxproxqp (declared there; listed so a --no-deps install still works)
equinox>=0.11.0
attrs>=23.1.0
loguru>=0.7.2
jaxproxqp @ git+https://github.com/oswinso/jaxproxqp.git
# GPU: pip install "jax[cuda12]>=0.5,<0.6"  (see README; 0.6.x crashes compiling the team_choiceseq3 guidance)
# STL robustness library (JAX fork), installed separately after CPU-only torch (see README):
# diff-spec @ git+https://github.com/jeappen/diff-spec.git@feature/jax
